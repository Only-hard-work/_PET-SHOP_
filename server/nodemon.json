{
  "watch": ["src"],
  "ext": "ts,json,env",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts", "dist"],
  "exec": "ts-node --files --transpile-only src/app.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
